<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipToPadding="false"
    android:padding="@dimen/activity_margin">

    <LinearLayout
        android:id="@+id/lyt_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="@dimen/activity_margin">

        <TextView
            android:id="@+id/tv_onboarder_title"
            style="@style/TextAppearance.AppCompat.Headline"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/white"
            tools:text="Section First"/>

        <TextView
            android:id="@+id/tv_onboarder_description"
            style="@style/TextAppearance.AppCompat.Body1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/activity_margin"
            android:alpha="0.7"
            android:gravity="center"
            android:textColor="@color/white"
            tools:text="A long description about my application"/>

    </LinearLayout>

    <ImageView
        android:id="@+id/iv_onboarder_image"
        android:layout_width="@dimen/section_image_item_width"
        android:layout_height="@dimen/section_image_item_height"
        android:layout_below="@id/lyt_text"
        android:adjustViewBounds="true"
        android:layout_centerInParent="true"
        android:scaleType="matrix" />


</RelativeLayout>